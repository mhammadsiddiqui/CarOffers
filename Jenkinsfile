pipeline {
    agent { 
        docker any
    /*{
            image 'node:22-alpine'}
    }
    stages {
        /*stage('pre-req') {
            steps {                
                sh 'sudo apt-get update'
                sh 'sudo apt-get install -y nodejs npm'
            }
        }*/
        stage('Build') { 
            steps {
                /*sh 'npm install'
                sh 'node -v'
                sh 'npm -v'*/
                echo 'nothing'
            }
        }
    }
}
